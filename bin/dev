#!/bin/bash

set -e

trap killgroup SIGINT

killgroup() {
  echo "Killing everything..."
  kill 0
}

SHELL=/bin/bash npx chokidar --initial -p "app/**/*" -c "bin/run" &
SHELL=/bin/bash npx chokidar --initial -p "app/css/*" -c "bin/build" &
wait

