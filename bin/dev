#!/bin/bash

set -e 
SCRIPT_DIR=$( cd -- "$( dirname -- "${0}" )" > /dev/null 2>&1 && pwd )
ROOT_DIR="${SCRIPT_DIR}/.."

RACK_ENV="${RACK_ENV:-development}"

RACK_ENV="${RACK_ENV}" foreman start --procfile "${ROOT_DIR}/Procfile.dev" --root "${ROOT_DIR}"

