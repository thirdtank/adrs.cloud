require "spec_helper"
RSpec.describe AuthenticatedAccount do
  it "should have tests" do
    expect(false).to eq(true)
  end
end
RSpec.describe AuthenticatedAccount::NoAccount do
  it "should have tests" do
    expect(false).to eq(true)
  end
end
